(function(global, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD
        define([], factory);
    } else if (typeof exports === 'object') {
        // CommonJS
        module.exports = factory();
    } else {
        // Global
        global.htmlspecialchars_decode = factory();
    }
})(this, function() {
    'use strict';

    return function(str) {
        if (str == null) return '';

        return String(str).
        replace('&amp;', '&').
        replace('&lt;', '<').
        replace('&gt;', '>').
        replace('&quot;', '"').
        replace('&#039;', '\'').
        replace('&lsqb;', '[').
        replace('&rsqb;', ']').
        replace('&Hat;', '^').
        replace('&sol;', '/').
        replace('&lpar;', '(').
        replace('&rpar;', ')').
        replace('&plus;', '+').
        replace('&bsol;', '\\').
        replace('&nbsp;', ' ').
        replace('&copy;', '©')


    };
});